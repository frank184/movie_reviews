<% flash.each do |name, msg| %>
  <% bootstrap_class = bootstrap_class_for(name) %>
  <div class="alert <%= bootstrap_class %>">
    <button class="close">x</button>
    <%= msg %>
  </div>
<% end %>
